{
  "schemaVersion": 1,
  "id": "espn.fantasy",
  "name": "ESPN Fantasy",
  "version": "0.1.0",
  "author": "tom",
  "license": "MIT",
  "description": "Live league-wide fantasy football scoreboard in the Omarchy bar.",
  "kinds": ["bar-widget"],
  "activation": "on-demand",
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "ESPN Fantasy",
    "description": "A football icon that opens every matchup in the league on hover: score, ESPN's win probability, who is on the field right now, and how much game clock is left.",
    "category": "Info",
    "aliases": ["espn", "fantasy", "football"],
    "allowMultiple": false,
    "defaults": {
      "refreshIntervalSec": 120,
      "hoverToOpen": true,
      "hoverDelayMs": 220
    },
    "schema": [
      { "key": "refreshIntervalSec", "type": "integer", "label": "Refresh interval (seconds)", "min": 30, "max": 1800, "step": 30, "defaultValue": 120, "description": "How often to re-poll ESPN. 120s keeps up with live scoring without hammering the API." },
      { "key": "hoverToOpen", "type": "boolean", "label": "Open on hover", "defaultValue": true, "description": "When off, the scoreboard opens on click only." },
      { "key": "hoverDelayMs", "type": "integer", "label": "Hover delay (ms)", "min": 0, "max": 1500, "step": 20, "defaultValue": 220, "description": "How long the pointer must rest on the icon before the scoreboard appears." }
    ]
  }
}
